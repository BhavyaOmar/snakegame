#include <stdio.h>
    int main(){
        
        int a=1, b=2, c=a+4, d=c+b;

        printf("The value of a is %d, while that of b is %d, in case you could consider value of c as %d, or at last don't forget the value of d is %d...",a,b,c,d);

    return 0;
}