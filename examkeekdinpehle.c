#include <stdio.h>
#include <math.h>

int main()
{
    // int n;
    // float sum = 0;

    // printf("Enter the value of n :\n");
    // scanf("%d", &n);

    // for (int i = 1; i <= n; i++)
    // {
    //     int fact = 1;
    //     for (int a = 1; a <= i + 1; a++)
    //     {
    //         fact *= a;
    //     }

    //     int j = pow(-1, i + 1);
    //     sum += j * i * i / fact;
    // }

    // Doosra Code @Kal ka experiment

    // printf("%f",sum);
    // int sum;
    // for (int i=1,sum=0;i<=500;sum+=i,i+=2);
    // printf("%d", sum);

    int a;
    scanf("%d", &a);

    

    return 0;
}
