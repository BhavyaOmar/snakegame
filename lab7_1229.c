#include <stdio.h>
// Implement Creation of BST without recursive function call. Write insert and delete functions.

typedef struct BST
{

    int data;
    struct BST *left;
    struct BST *right;
} node;

node *create()
{
    node *temp;
    printf("Enter the data");
    temp = (node)
}
int main()
{

    return 0;
}